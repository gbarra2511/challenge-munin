# frontend/

Scaffold real do Next.js entra no Dia 4 do cronograma (ver `../PLANO.md`).

Comando previsto:

```
pnpm create next-app@latest . --typescript --tailwind --eslint --app \
  --src-dir --import-alias "@/*"
```

Antes disso, invocar `/hallmark` para gerar tokens e princípios de design.
